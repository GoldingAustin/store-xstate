---
language: typescript
tasks:
  build:
    command:
      - vite
      - build
    deps:
      - 'legend-xstate:build-dep1'
  build-dep1:
    command: tsc
  dev:
    command: vite
    local: true
  test:
    command:
      - vitest
      - run
